Witaj,

pojawiły się nowe dokumenty spełniające zasubskrybowane przez Ciebie kryteria wyszukiwania:
<% data.forEach(({ searchPhrase, documentSource, lastNotifyFormatted }) => { %>
<% if (searchPhrase) { %>
* <%= searchPhrase %>: <%= mainUrl %>?search="<%= searchPhrase %>"&dateFrom=<%= lastNotifyFormatted %>
<% } else { %>
* <%= documentSource %>: <%= mainUrl %>?sourceName=<%= encodeURI(documentSource) %>&dateFrom=<%= lastNotifyFormatted %>
<% } %>
<% }) %>
Pozdrawiamy,
zespół OtwartePaństwo.pl

